# Carousel

A horizontally scrollable component for browsing a small set of items (e.g. bank products).

## Variants

- `default-default`: baseline layout
- `bank-products`: bank product carousel (`carousel-bank-products` export)

## States

- `default`: exported snapshot (no hover/pressed exports yet)

