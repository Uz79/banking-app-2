# Payment flow — type ahead search (active)

Search field focused with query / results visible.

## Variants

- `mobile-default` — Mobile vector.

## States

- `default`: exported snapshot from Figma (see also `interactionStates` in JSON).

## Implementation

Maps to hash segment `recipient-search` (`data-step="recipient-search"`).

