CardAmount from @banking-app/ui. Use via `window.BankingAppUI.CardAmount` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<ThemeRoot>` (full provider chain in README.md — components read theme/i18n from that context).

## Props

```ts
interface CardAmountProps {
  currency: React.ReactNode;
  value: React.ReactNode;
}
```
