# Selection list item

List item patterns optimized for selection lists (e.g. country picker).

## Variants

Variants are organized as `variants/<type>-<case>/...` based on the Figma export metadata.

## States

- `default`
- `hover` (when exported)
- `focus` (exported as `Pressed` in Figma)

