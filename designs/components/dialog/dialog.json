{
  "name": "dialog",
  "category": "dialog",
  "description": "Modal dialog for confirmations or focused tasks.",
  "interactionStates": [
    "default",
    "hover",
    "focus"
  ],
  "assets": {
    "variants": {
      "default-default": {
        "default": "variants/default-default/default.svg"
      },
      "basic": {
        "default": "variants/basic/default.svg",
        "default 2": "variants/basic/default 2.svg"
      },
      "confirmation": {
        "default": "variants/confirmation/default.svg",
        "default 2": "variants/confirmation/default 2.svg"
      }
    },
    "overview": "overview.png"
  },
  "tokens": {
    "radius": "alias.radius.regular",
    "modes": {
      "light": {
        "foreground": "mapped/light.foreground.foreground"
      },
      "dark": {
        "foreground": "mapped/dark.foreground.foreground"
      }
    }
  },
  "notes": [
    "Figma exports `Pressed` are stored as `focus.svg` for consistency."
  ]
}
