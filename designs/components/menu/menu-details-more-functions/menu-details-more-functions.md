# Menu — details / more functions

Expanded functions from account details.

## Variants

- `default-default` — Vector snapshot.

## States

- `default`: exported snapshot from Figma (see also `interactionStates` in JSON).
