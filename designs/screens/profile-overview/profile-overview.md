# Profile — overview

Profile settings tab strip / overview.

## Variants

- `default-default` — Vector snapshot (`default.svg`), same convention as other `profile-*` screens.

## States

- `default`: exported snapshot from Figma (see also `interactionStates` in JSON).
