# Basic dialog

Neutral modal framing pattern.

## Variants

- `default-default` — Baseline vector chrome + content.

## States

- `default`: exported snapshot from Figma (see also `interactionStates` in JSON).
